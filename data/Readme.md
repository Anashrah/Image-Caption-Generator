# Dataset Information

This project uses an existing image caption dataset, "COCO Image Captioning Dataset," downloaded from Kaggle. We use the caption_train2017 for training and caption_val2017 for evaluation. Each image already includes multiple human‑written captions, which we use for training, validation, and testing. Before training, the data is cleaned (checking for missing files, duplicates, or corrupted images). 

